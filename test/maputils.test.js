// mapUtils.test.js
import { searchForLicensePlate, removeMarker } from 'pubilc/src/js/utils/maputils';

// Jest.mock() und andere Hilfsfunktionen können hier hinzugefügt werden, um die Tests zu unterstützen

test('searchForLicensePlate finds the correct marker', () => {
  // Set up your test data and mock functions

  // Call the searchForLicensePlate function with the test data

  // Check if the expected outcome is correct, e.g., the map view is changed or an alert is shown
});

test('removeMarker removes the marker and deletes it from the markers map', () => {
  // Set up your test data and mock functions

  // Call the removeMarker function with the test data

  // Check if the marker is removed from the map and deleted from the markers map
});
